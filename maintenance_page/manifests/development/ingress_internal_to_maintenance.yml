apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: check-performance-data-development.test.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: check-performance-data-development.test.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: check-performance-data-maintenance
            port:
              number: 80
