apiVersion: apps/v1
kind: Deployment
metadata:
  name: check-performance-data-maintenance
  labels:
    app: check-performance-data-maintenance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: check-performance-data-maintenance
  template:
    metadata:
      labels:
        app: check-performance-data-maintenance
    spec:
      nodeSelector:
        teacherservices.cloud/node_pool: applications
        kubernetes.io/os: linux
      containers:
      - name: check-performance-data-maintenance
        image: ghcr.io/dfe-digital/check-performance-data-maintenance:#MAINTENANCE_IMAGE_TAG#
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 64M
            cpu: 0.1
          limits:
            memory: 64M
            cpu: 1
