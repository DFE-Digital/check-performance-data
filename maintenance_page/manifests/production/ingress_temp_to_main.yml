apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: check-performance-data-temp.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: check-performance-data-temp.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: check-performance-data-production
            port:
              number: 80
