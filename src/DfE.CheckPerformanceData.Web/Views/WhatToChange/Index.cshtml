@model DfE.CheckPerformanceData.Web.Controllers.WhatToChangeViewModel;
@using DfE.CheckPerformanceData.Domain.Enums
@using DfE.CheckPerformanceData.Web.Controllers.Journey

@{
    // Matches the <h1> the radios legend renders (is-page-heading="true") — same words, and the
    // same noun, which is "student" on a 16-19 window.
    ViewBag.Title = $"What {Model.Noun.Singular} data would you like to change?";
    Layout = "_Layout";
}

<a asp-action="Index" asp-controller="CheckYourPupilData" asp-route-windowId="@Model.WindowId" class="govuk-back-link">Back</a>

<form method="post" action="@Url.Action("Index", "WhatToChange", new { windowId = Model.WindowId })">
    @Html.AntiForgeryToken()
    <govuk-radios for="SelectedWhatToChange">
        <govuk-radios-fieldset>
            <govuk-radios-fieldset-legend is-page-heading="true" class="govuk-fieldset__legend--l">
                What @Model.Noun.Singular data would you like to change?
            </govuk-radios-fieldset-legend>

            <govuk-radios-hint>
                Select one option.
            </govuk-radios-hint>

            <govuk-radios-item value="@WhatToChange.Merge">Merge duplicate @Model.Noun.Singular records</govuk-radios-item>
            <govuk-radios-item value="@WhatToChange.Include">Include a @Model.Noun.Singular in data</govuk-radios-item>
            <govuk-radios-item value="@WhatToChange.Remove">Remove a @Model.Noun.Singular from data</govuk-radios-item>
            @if (Model.CheckingWindowType is { } windowType
                && AddPupilJourney.SupportedWindowTypes.Contains(windowType))
            {
                <govuk-radios-item value="@WhatToChange.Add">Add a @Model.Noun.Singular to data</govuk-radios-item>
            }
        </govuk-radios-fieldset>
    </govuk-radios>

    <govuk-button type="submit">Continue</govuk-button>
</form>